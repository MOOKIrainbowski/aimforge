// Intentionally empty. AimForge's game code (core/) is plain browser
// JavaScript shared verbatim with the web build — it reads/writes
// localStorage and uses pointer lock, both available to the renderer
// without any Node/Electron API bridged in here.
